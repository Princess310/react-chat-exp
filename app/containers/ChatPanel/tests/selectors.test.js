// import { fromJS } from 'immutable';
// import { makeSelectChatPanelDomain } from '../selectors';

// const selector = makeSelectChatPanelDomain();

describe('makeSelectChatPanelDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
